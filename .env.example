# Application under test
BASE_URL=https://the-internet.herokuapp.com/

# Set by CI — adjusts workers and retries in playwright.config.ts
CI=false

# Auth scenarios (never commit real values)
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
